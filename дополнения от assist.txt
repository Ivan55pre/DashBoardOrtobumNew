Как я могу добавить систему приглашений по email для пользователей, которых еще нет в системе?


Рефакторинг: вынеси логику загрузки и обработки данных из компонента `CashBankReport` в кастомный хук `useCashBankReport`.

Вынеси повторяющуюся логику загрузки данных из компонентов отчетов в кастомный хук `useReportData`.

+ Добавь возможность изменять порядок виджетов на дашборде с помощью drag-and-drop и сохранять его для пользователя.
npm install @dnd-kit/core @dnd-kit/sortable

+ Добавь возможность скрывать и показывать виджеты на дашборде через меню настроек.

В моих компонентах много повторяющегося кода для загрузки данных и обработки ошибок. Можешь предложить способ рефакторинга с использованием кастомных хуков?

Как я могу сгенерировать актуальные TypeScript-типы для моей схемы Supabase, чтобы избежать подобных ошибок в будущем?

Как я могу настроить ESLint для автоматического удаления неиспользуемых импортов при сохранении файла?


*************
Интерфейс `InventoryTurnoverData` содержит поле `organization_name`, которого нет в базе данных. Как мне правильно отрефакторить компонент, чтобы он работал с реальными данными?
***********

Как можно улучшить SQL-функцию `upsert_cash_bank_report`, чтобы она была более производительной при работе с большим объемом данных в JSON?

!!!
проверь приложение. Цель: 1. чтобы пользователь мог смотреть отдельные отчеты не только по отдельной, доступной организации, а по всем доступным сразу (консолидироованные данные) или по одной на выбор (нужно для детального анализа). 2. а страница dashboard имела отбор, где можно указать организацию - все доступные, отдельную или набрать список из доступных. Данные по указанным организациям отражались в виджетах.

!!!
Улучши внешний вид фильтра организаций, заменив `<select>` на выпадающий список с чекбоксами.

В компоненте `DebtReport.tsx` используется `loadSampleData`. Можно ли улучшить логику загрузки данных, чтобы избежать показа демонстрационных данных, если реальные данные просто отсутствуют на выбранную дату?

Теперь, когда все отчеты используют единый паттерн, давай создадим компонент "Сводная панель" (Dashboard), который будет отображать ключевые показатели со всех отчетов на одном экране.

Создай новый компонент `DashboardSummaryCard`, который будет принимать данные из хука `useReportItems` и отображать итоговые значения для одного отчета (например, "Итого на счетах" или "Общая задолженность").

Как можно рефакторить хук `useReportItems.ts`, чтобы избежать `JSON.stringify` в зависимостях `useEffect`?

Реализуй экспорт отчета в формат XLSX, сохраняя иерархию и стили.

Давай аналогичным образом переделаем отчет по задолженностям (DebtReport), используя `client_id` и `parent_client_id`.

Проанализируй другие `upsert_*_report` функции и предложи аналогичные улучшения с использованием CTE.

Реализуй в отчете `CashBankReport` логику для отображения итоговых сумм по каждой организации в консолидированном виде.

Можно ли добавить на дашборд график, показывающий динамику денежных средств за последний месяц? - интервал

В виджете "Задолженности" цвет суммы просроченной задолженности должен быть красным. Сделай это.

Реализуй сохранение настроек в компоненте NotificationSettingsCard.

Создай глобальный контекст для хранения активной организации и используй его в UserProfileCard.

Добавь поддержку PDF в компонент `FileUpload`.


Добавь в виджеты отображение динамики по сравнению с предыдущим днем.

ВАЖНО!
Проанализируй использование parent_id в разных таблицах и предложи улучшения для консистентности.
- будет изменения, но делать , когда работаем с загрузкой данных
